[pytest]
testpaths = tests
markers =
    gpu: marks tests that require CUDA GPU
    slow: marks tests that are slow (load checkpoints, etc.)
    perf: marks performance/profiling tests (require demo video downloads)
